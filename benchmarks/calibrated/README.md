# Calibrated Benchmark Adapters

This directory is reserved for adapters that deliberately use dataset-specific
aliases, caches, normalization or supervision. Every result must declare the
`calibrated` lane in its evidence manifest and must never be presented as
default Bilinc product-core performance.

No calibrated adapter is currently canonical.
